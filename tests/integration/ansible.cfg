[defaults]
inventory = inventory/test_inventory.customapi.yml
# 이 ansible.cfg에서 세 단계 위 (프로젝트 루트)에 있는 plugins/inventory를 참조
inventory_plugins = ../../plugins/inventory
host_key_checking = False

[inventory]
# 플러그인 파일 내 NAME 속성 사용
enable_plugins = custom_api_inventory